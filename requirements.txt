# requirements.txt — Redrob AI Hackathon
# All packages are CPU-compatible; no GPU libraries required.
# No network calls are made during ranking (offline pre-computation only).

sentence-transformers==2.7.0    # bi-encoder (all-MiniLM-L6-v2) + cross-encoder (ms-marco-MiniLM-L-6-v2)
rank-bm25==0.2.2                # BM25 lexical scoring
numpy==1.26.4                   # fast matrix ops (cosine sim via matmul)
pandas==2.2.2                   # data manipulation + CSV output
python-docx==1.1.0              # JD parsing from .docx file
scikit-learn==1.4.2             # utility functions (optional; used via sentence-transformers)
tqdm==4.66.4                    # progress bars for pre-computation
PyYAML==6.0.1                   # metadata YAML loading
streamlit==1.35.0               # sandbox demo (sandbox/app.py only)
torch==2.3.0                    # required by sentence-transformers (CPU build)
transformers==4.41.0            # required by sentence-transformers
tokenizers==0.19.1              # required by transformers
